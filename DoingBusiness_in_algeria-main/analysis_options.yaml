# ════════════════════════════════════════════════════════════════════════
#  Dart Analyzer & Linter — strict config
# ════════════════════════════════════════════════════════════════════════
#  Catches:
#    - print() in production code
#    - unused imports / variables
#    - missing const / unnecessary new
#    - missing final / prefer_final_fields
#    - TODOs that never close
# ════════════════════════════════════════════════════════════════════════

include: package:flutter_lints/flutter.yaml

analyzer:
  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true

  errors:
    # These go from warnings to hard errors — CI fails on them.
    invalid_assignment: error
    missing_required_param: error
    missing_return: error
    avoid_print: error
    unused_import: error
    unused_local_variable: warning
    dead_code: error

  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"
    - "lib/firebase_options.dart"   # generated
    - "lib/testing/**"               # scratch area

linter:
  rules:
    # ─── Production hygiene ──────────────────────
    avoid_print: true
    avoid_web_libraries_in_flutter: true
    prefer_const_constructors: true
    prefer_const_literals_to_create_immutables: true
    prefer_const_declarations: true
    prefer_final_fields: true
    prefer_final_locals: true
    unnecessary_new: true
    unnecessary_const: true
    unnecessary_this: true
    unnecessary_null_checks: true
    unnecessary_parenthesis: true
    unnecessary_brace_in_string_interps: true

    # ─── Error-prone patterns ────────────────────
    avoid_empty_else: true
    avoid_returning_null_for_future: true
    avoid_slow_async_io: true
    cancel_subscriptions: true
    close_sinks: true
    control_flow_in_finally: true
    empty_catches: true
    no_adjacent_strings_in_list: true
    throw_in_finally: true
    unrelated_type_equality_checks: true
    use_build_context_synchronously: true
    valid_regexps: true

    # ─── Style ──────────────────────────────────
    always_declare_return_types: true
    always_use_package_imports: true
    camel_case_types: true
    camel_case_extensions: true
    constant_identifier_names: false  # allow SCREAMING_SNAKE for constants
    file_names: true
    library_names: true
    non_constant_identifier_names: true
    prefer_single_quotes: true
    require_trailing_commas: true
    sort_child_properties_last: true
